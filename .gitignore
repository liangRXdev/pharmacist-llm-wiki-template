# ── 原始文獻（受著作權保護，切勿上傳）─────────────────
raw/*
!raw/.gitkeep
!raw/finish/
raw/finish/*
!raw/finish/.gitkeep
!raw/assets/
raw/assets/*
!raw/assets/.gitkeep

# ── PDF 提取暫存 ───────────────────────────────────
output/
*_extracted.txt
*.tmp

# ── 你的個人 wiki 內容（白名單邏輯：預設全部忽略）──────
# wiki/ 內所有檔案一旦 ingest 即為受著作權保護的摘要/衍生著作，
# 預設不追蹤，杜絕誤 commit 上 public repo（見 DISCLAIMER.md）。
# 空骨架放在 Templates/index.md、Templates/log.md，首次使用時複製進來。
wiki/*
!wiki/.gitkeep
# 要把本資料夾當「私有知識庫」用 git 備份：請改用 private repo，
# 再以 `git add -f wiki/你的檔案.md` 個別強制追蹤。

# ── Obsidian / OS / 編輯器 ─────────────────────────
.obsidian/
.DS_Store
Thumbs.db
*.swp
.vscode/
